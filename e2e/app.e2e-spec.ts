import { AngularCliLazySamplePage } from './app.po';

describe('angular-cli-lazy-sample App', () => {
  let page: AngularCliLazySamplePage;

  beforeEach(() => {
    page = new AngularCliLazySamplePage();
  });

  // it('should display welcome message', () => {
  //   page.navigateTo();
  //   expect(page.getParagraphText()).toEqual('Welcome to app!');
  // });
});
